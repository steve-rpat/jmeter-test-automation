on: [push]
jobs:
  Main:
    name: JMeter CICD
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - name: Run JMeter Tests
        uses: QAInsights/PerfAction
        with:
          test-plan-path: Bundle-Engine-Test/Bundle-Engine-XO-Test.jmx
          args: ""
      - name: Upload Results
        uses: actions/upload-artifact
        with:
          name: jmeter-results
          path: result.jtl
